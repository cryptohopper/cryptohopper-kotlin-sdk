package com.cryptohopper

/**
 * SDK version. Kept in sync with `build.gradle.kts` `version` and the git
 * tag by the release workflow's parity check.
 */
public const val SDK_VERSION: String = "0.1.0-alpha.1"
